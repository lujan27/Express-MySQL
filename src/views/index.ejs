<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Compiled and minified CSS -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css">
    <title>Document</title>
</head>
<body>
    <div class="container-fluid">
        <div class="row">
            <div class="col s6">
                
                <h4>Your data:</h4>
                
                <% if(people != ''){ %>
                <table class="responsive-table">
                    <%people.map(person => {%>
                    <thead>
                      <tr>
                            <th>ID</th>
                            <th>Name</th>
                            <th>Age</th>
                            <th>Job</th>
                            <th>Actions:</th>
                      </tr>
                    </thead>
            
                    <tbody>
                      <tr>
                        <td><%=person.id%></td>
                        <td><%=person.name%></td>
                        <td><%=person.age%></td>
                        <td><%=person.job%></td>
                        <td style="display: flex; justify-content: center; align-items: center;">
                            <a href="/person/<%=person.id%>" class="waves-effect waves-light btn">Update</a>
                            <form action="/delete/<%=person.id%>?_method=DELETE" method="POST">
                                <button type="submit" class="waves-effect waves-light btn red">Eliminar</button>
                            </form>
                        </td>
                      </tr>
                    </tbody>
                    <% }) %>
                  </table>
                <% } else { %>
                    <h5>No data yet</h5>
                <% } %>    
            </div>
            <div class="col s6">
                <h4>New Person:</h4>

                <form action="/add" method="post">
                    <div class="input-field col s12">
                        <input id="name" type="text" class="validate" name="name" required>
                        <label for="name">Name:</label>
                        <span class="helper-text" data-error="wrong" data-success="right">Type your name</span>
                    </div>
                    <div class="input-field col s12">
                        <input id="age" type="number" class="validate" name="age" required>
                        <label for="age">Age:</label>
                        <span class="helper-text" data-error="wrong" data-success="right">Type your age</span>
                    </div>
                    <div class="input-field col s12">
                        <input id="job" type="text" class="validate" name="job" required>
                        <label for="job">Job:</label>
                        <span class="helper-text" data-error="wrong" data-success="right">Type your job</span>
                    </div>
                    <button class="btn waves-effect waves-light" type="submit" name="action">
                        Submit
                    </button>
                </form>
                
            </div>
        </div>
    </div>
</body>
<!-- Compiled and minified JavaScript -->
<script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"></script>
</html>