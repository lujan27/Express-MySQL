<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Compiled and minified CSS -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css">
    <title>Document</title>
</head>
<body>
    <div class="container">
        <div class="row">
            <%info.map(person => {%>
                <h4>Information about: <%=person.name%></h4>
                <form action="/update/<%=person.id%>?_method=PUT" method="post">
                    <div class="input-field col s12">
                        <input id="name" type="text" class="validate" name="name" value="<%=person.name%>">
                        <label for="name">Name:</label>
                        <span class="helper-text" data-error="wrong" data-success="right">Type your name</span>
                    </div>
                    <div class="input-field col s12">
                        <input id="age" type="number" class="validate" name="age" value="<%=person.age%>">
                        <label for="age">Age:</label>
                        <span class="helper-text" data-error="wrong" data-success="right">Type your age</span>
                    </div>
                    <div class="input-field col s12">
                        <input id="job" type="text" class="validate" name="job" value="<%=person.job%>">
                        <label for="job">Job:</label>
                        <span class="helper-text" data-error="wrong" data-success="right">Type your job</span>
                    </div>
                    <button class="btn waves-effect waves-light" type="submit" name="action">
                        Submit
                    </button>
                </form>
                <% }) %>
        </div>
    </div>
</body>
<!-- Compiled and minified JavaScript -->
<script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"></script>
</html>